# 비워두면 같은 오리진(/api)으로 요청한다.
#  - 로컬 개발: vite dev server가 /api 를 백엔드로 프록시 (vite.config.ts)
#  - Vercel 배포: vercel.json 의 rewrites 가 /api 를 백엔드로 프록시
# 백엔드를 직접 호출하고 싶을 때만 값을 채운다. (백엔드 CORS 허용 필요)
VITE_API_BASE_URL=

# 로컬 dev server 프록시 대상 (기본값: 배포된 EC2)
VITE_DEV_PROXY_TARGET=http://13.124.207.246:8080
