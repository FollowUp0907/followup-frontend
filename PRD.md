# PRD — FollowUp

## 1. Document Information

| 항목 | 내용 |
| --- | --- |
| 프로젝트명 | **FollowUp** |
| 문서명 | Product Requirements Document |
| 버전 | **v2.0 (구현 반영본)** |
| 최초 작성 | 2026.09 |
| 갱신일 | **2026.09.22** |
| 작성자 | 반서현, 장은호 |
| 개발 인원 | 2명 (Frontend 1 · Backend 1) |
| 개발 기간 | **2026.09.10 – 09.22 (13일, 진행 중)** |
| 1일 작업 시간 | 약 7시간 |
| Frontend | React 18 · TypeScript · Vite · Tailwind |
| Backend | Spring Boot · EC2 (HTTPS) |
| Database | MySQL |
| AI | LLM API |
| 배포 | Frontend: Vercel · Backend: EC2 |
| 운영 주소 | https://followup-frontend-pied.vercel.app |

> **v1.0 대비 주요 변경**
> ① 제품명을 **FollowUp** 으로 통일 (v1.0 문서 18장의 "AfterMeet" 은 오기)
> ② Could / Phase 2 로 잡았던 **음성 인식·알림**이 MVP 기간 안에 구현됨
> ③ **다중 담당자, 이메일 초대, 구글 로그인, 웹 푸시**가 새로 추가됨
> ④ 데이터 모델·상태값을 실제 API 규격에 맞춰 수정

---

## 2. Product Overview

### 한 줄 소개

> **회의록을 입력하면 AI가 결정 사항과 후속 업무를 구조화하고, 담당자·기한·우선순위를 정리하여 실제 업무 진행과 다음 회의까지 연결해 주는 AI 기반 협업 서비스**

### 핵심 가치

> **회의를 기록하는 것에서 끝나지 않고, 회의에서 나온 일이 실제 실행으로 이어지도록 관리한다.**

### 서비스 핵심 흐름

```
회의 생성 (참여자 선택 · 이전 미완료 업무 이어받기)
    ↓
회의록 입력 (붙여넣기 또는 음성 받아쓰기)
    ↓
AI 분석
    ↓
결정 사항 / 후속 업무 구조화 (초안)
    ↓
사용자 검토·수정 (초안 ↔ 확정본 비교)
    ↓
후속 업무 확정
    ↓
업무 진행 관리 + 자동 알림
    ↓
다음 회의 → 이전 미완료 업무 확인
```

---

## 3. Background / Problem

회의를 진행한 후 회의록을 남기더라도 실제 업무 관리까지 자연스럽게 연결되지 않는다.
자유롭게 작성된 회의록에는 결정 사항, 의견, 일정, 담당 업무가 섞여 있어 회의가 끝난 뒤 다시 정리해야 한다.

### 주요 문제

- 결정 사항과 단순 의견이 섞여 있다.
- 누가 무엇을 해야 하는지 명확하지 않다.
- 업무 마감일이 회의록 속 문장으로만 남는다.
- 후속 업무가 메신저, 문서, 개인 메모에 흩어진다.
- 이전 회의 업무의 완료 여부를 추적하기 어렵다.
- 다음 회의에서 지난 진행 상황을 다시 확인해야 한다.

### 근거 (정량)

| 지표 | 값 | 출처 |
| --- | --- | --- |
| 직장인 주간 회의 횟수 | 3.7회 | 대한상공회의소 「국내 기업의 회의문화 실태와 개선해법」(2017) |
| 회의 1회 평균 시간 | 51분 | 〃 |
| "불필요했다"고 답한 회의 | 1.8회 (약 절반) | 〃 |
| 회의문화 자체 평가 | 100점 만점에 45점 | 〃 |

> 2017년 조사라는 점은 감안해야 한다. 최신 국내 공개 조사를 찾지 못해 그대로 인용한다.

### Core Problem

> **회의 내용은 기록되지만, 회의에서 결정된 업무가 실제 실행까지 이어지는 과정은 체계적으로 관리되지 않는다.**

| Before | After |
| --- | --- |
| 회의 → 회의록 작성 → 메신저/문서/개인 메모 → 담당자·기한 재확인 → **업무 누락** | 회의 → AI 구조화 → 사용자 검토 → 후속 업무 생성 → **상태 관리 + 자동 알림** → 다음 회의 연결 |

---

## 4. Product Goal

| # | 목표 | 현재 상태 |
| --- | --- | --- |
| G1 | 비정형 회의록에서 결정 사항·후속 업무·담당자·기한을 자동 구조화 | **구현 완료** |
| G2 | 회의 내용을 요약에서 끝내지 않고 실제 후속 업무로 전환 | **구현 완료** |
| G3 | AI 결과를 즉시 확정하지 않고 사용자 검토·수정 후 등록 | **구현 완료** (초안 ↔ 확정본 비교 화면) |
| G4 | 이전 회의의 미완료 업무를 다음 회의에서 다시 확인 | **구현 완료** (`carryOverActionItemIds`) |
| G5 | 업무 상태·마감 임박·지연을 프로젝트 단위로 가시화 | **구현 완료** (대시보드) |
| **G6** | **회의 밖에서도 업무가 잊히지 않게 한다** | **신규 — 구현 완료** (알림 SSE + 웹 푸시) |

> G6 은 v1.0 에 없던 목표다. 개발 중 "화면을 열어야만 지연을 알 수 있다"는 문제가 드러나 MVP 범위로 끌어올렸다.

---

## 5. Target User

### Primary Target — 소규모 프로젝트 및 협업 팀

개발 프로젝트 팀 · 대학생 팀 프로젝트 · 사이드 프로젝트 팀 · 스터디 팀 · 소규모 스타트업 실무 팀

### User Characteristics

- 정기적으로 회의하고, 회의록을 자유 형식으로 기록한다.
- 회의 후 여러 개의 후속 업무가 발생한다.
- 전담 PM 이 없어 "회의가 끝나면 누가 정리하는가"가 매번 문제가 된다.
- 복잡한 프로젝트 관리 도구보다 간단한 업무 관리 방식을 선호한다.

### 규모 가정

프로젝트당 **최대 10명** 을 전제로 설계했다. 그 이상 규모에서는 권한 체계와 알림 정책이 달라져야 한다 (Out of Scope).

---

## 6. Core User Scenario

```
로그인 (이메일 또는 구글 계정)
  ↓
프로젝트 선택 / 생성
  ↓
구성원 초대 (이메일 — 미가입자도 가능)
  ↓
회의 생성 (참여자 선택 + 이전 미완료 업무 이어받기)
  ↓
회의록 입력 (붙여넣기 / 음성 받아쓰기)
  ↓
AI 분석 요청
  ↓
분석 결과 초안 생성 (PROCESSING → GENERATED)
  ↓
사용자 검토·수정 → 확정 (CONFIRMED)
  ↓
후속 업무 생성 (담당자 복수 지정 가능)
  ↓
예정 / 진행 중 / 완료 상태 관리
  ↓
마감 D-7부터 매일 · 지연 시 상시 알림
  ↓
다음 회의 생성 → 이전 미완료 업무 확인
```

### Example

**입력**

> 김 대리는 금요일까지 로그인 오류를 수정하기로 했다.
> 배포는 다음 주 월요일에 진행한다.
> 검색 기능은 이번 버전에서 제외하기로 결정했다.

**AI 분석 결과 (초안)**

| 항목 | 내용 |
| --- | --- |
| 후속 업무 | 로그인 오류 수정 |
| 담당자 (AI 추정 이름) | 김 대리 |
| 마감일 | 금요일 → 회의 날짜 기준 절대 날짜로 변환 |
| 우선순위 | HIGH |
| 우선순위 이유 | 배포 전 해결이 필요한 오류 |
| 결정 사항 | 검색 기능은 이번 버전에서 제외 |

**확정 시 처리**

AI 가 넘긴 `assigneeName`(자유 문자열)을 프로젝트 구성원과 이름으로 매칭해 실제 `assigneeUserIds`(복수)로 바꾼 뒤 저장한다. 매칭에 실패하면 미지정으로 두고 사용자가 직접 고른다.

---

## 7. Scope — 계획 대비 실제

| 우선순위 | 기능 | v1.0 계획 | 현재 |
| --- | --- | --- | --- |
| Must | 회원가입 / 로그인 | Must | **완료** |
| Must | 프로젝트 생성·조회 | Must | **완료** |
| Must | 회의 생성·조회 | Must | **완료** |
| Must | 회의록 작성·수정 | Must | **완료** |
| Must | AI 회의록 분석 | Must | **완료** |
| Must | 결정 사항 / 후속 업무 / 담당자 / 마감일 추출 | Must | **완료** |
| Must | AI 결과 검토·수정 | Must | **완료** |
| Must | 후속 업무 생성·상태 관리 | Must | **완료** |
| Must | 이전 회의 미완료 업무 조회·연결 | Must | **완료** |
| Should | AI 우선순위 추천 + 이유 | Should | **완료** (`priorityReason`) |
| Should | 프로젝트 대시보드 | Should | **완료** |
| Should | 마감 임박 / 지연 업무 표시 | Should | **완료** |
| Should | 담당자별 업무 필터 | Should | **완료** |
| Could | 음성 인식 | Could (Phase 2) | **완료 — 앞당김** (Web Speech API, 서버 불필요) |
| Could | 서비스 내 알림 | Could (Phase 2) | **완료 — 앞당김** (SSE 실시간 + 폴링 안전망) |
| — | **다중 담당자** | 없음 | **추가·완료** |
| — | **이메일 구성원 초대 (미가입자 포함)** | 없음 | **추가·완료** |
| — | **구글 로그인** | 없음 | **추가·완료** |
| — | **웹 푸시 (앱 종료 상태 알림)** | 없음 | **추가·완료 — 실사용 검증 대기** |
| — | **프로젝트 나가기 / 구성원 10명 제한** | 없음 | **추가·완료** |
| — | 계정 이름 변경 · 회원 탈퇴 | 없음 | **프론트 완료 · 서버 API 대기** |
| Could | 회의록 파일 업로드 | Could | 미구현 |
| Could | 이메일 업무 알림 | Could | 미구현 (초대 메일만 발송) |
| Could | AI 다음 회의 안건 추천 | Could | 미구현 |
| Could | 프로젝트 회고 요약 | Could | 미구현 |

---

## 8. Core Features

### F01. 프로젝트 관리
생성 · 목록 · 상세 · 수정 · 삭제. 프로젝트별 회의·후속 업무·구성원 관리.
소유자(OWNER)와 구성원(MEMBER) 두 단계 권한. 구성원은 **프로젝트 나가기**, 소유자는 **프로젝트 삭제**.

### F02. 구성원 관리 *(신규)*
- 이메일로 초대 → 초대 메일 발송 → 받은 사람이 **수락해야 합류**
- **FollowUp 계정이 없어도 초대 가능** — 메일 링크로 가입하면 그 프로젝트에 자동 합류
- 수락 대기 중인 초대는 구성원 목록에 표시되고 **재발송 / 취소** 가능
- 정원은 **구성원 + 대기 중 초대 합산 10명**

### F03. 회의 관리
생성 · 목록 · 상세 · 회의록 작성/수정 · 삭제.
생성 시 **참여자 선택**과 **이전 회의 미완료 업무 이어받기**(`carryOverActionItemIds`)를 함께 지정한다.
회의 상태는 `DRAFT`(분석 전) / `CONFIRMED`(확정 완료).

### F04. 회의록 입력 — 음성 받아쓰기 *(앞당겨 구현)*
브라우저 Web Speech API 로 말하는 대로 회의록 칸에 실시간 입력된다. 별도 서버·음성 파일 저장 없음.

### F05. AI 회의록 분석
AI 가 회의록에서 다음을 구조화한다 — 결정 사항 / 후속 업무 / 담당자(이름) / 마감일 / 우선순위 / 우선순위 이유.
분석 상태: `PROCESSING` → `GENERATED` → `CONFIRMED`, 실패 시 `FAILED`(재시도 가능).

### F06. AI 분석 결과 검토
AI 결과를 바로 저장하지 않는다.

```
AI 분석 → 초안(GENERATED) → 사용자 검토·수정 → 확정(CONFIRMED) → 후속 업무 생성
```

확정 직전 **AI 초안과 사용자가 고친 확정본을 나란히 비교**해 보여주고 확인을 받는다.
수정 가능 항목: 후속 업무명 · 설명 · 담당자(복수) · 마감일 · 우선순위 · 결정 사항.

### F07. 후속 업무 관리
업무명 · 설명 · **담당자(복수)** · 생성 회의 · 마감일 · 우선순위 · 우선순위 이유 · 상태.
상태: `TODO` 예정 / `IN_PROGRESS` 진행 중 / `DONE` 완료.
보드 보기(끌어서 상태 변경)와 목록 보기, 회의·담당자·우선순위·마감 필터.

### F08. 이전 회의 → 다음 회의 연결
이전 회의에서 생성된 미완료 업무를 새 회의 작성 화면에서 확인하고 이어받는다.

### F09. 프로젝트 대시보드
전체 / 예정 / 진행 중 / 완료 / 지연 업무 수, 마감 임박 업무, 지연 업무, 최근 회의, 담당자별 진행률.

### F10. 알림 *(앞당겨 구현)*

| 종류 | 발생 시점 |
| --- | --- |
| `TASK_CREATED` | 나에게 업무가 배정됨 |
| `TASK_UPDATED` | 담당 업무가 수정됨 |
| `TASK_COMPLETED` | 담당 업무가 완료 처리됨 |
| `DUE_SOON` | 마감 **D-7부터 매일 한 번** |
| `OVERDUE` | 마감일이 지난 업무 — 해결될 때까지 계속 |
| `MEMBER_JOINED` | 초대받은 사람이 수락해 합류 *(프론트 준비 완료, 서버 발송 대기)* |

전달 경로는 셋이다.

1. **SSE** — 앱이 열려 있을 때 즉시 (프록시를 거치면 스트림이 고여 백엔드에 직접 연결)
2. **폴링** — SSE 가 끊겼을 때를 위한 안전망 (연결 시 5분 간격까지 완화)
3. **웹 푸시(FCM)** — 앱을 완전히 꺼 둔 동안

### F11. 계정 *(신규, 서버 대기)*
프로필에서 이름 변경과 회원 탈퇴. 탈퇴는 **본인 이메일을 그대로 입력해야** 진행된다.
현재 서버 API(`PATCH`/`DELETE /api/users/me`)가 없어 "준비되지 않았습니다"로 안내한다.

---

## 9. AI Classification Category

| Category | 설명 | 예시 |
| --- | --- | --- |
| DECISION | 회의에서 최종 결정된 사항 | 검색 기능은 이번 버전에서 제외한다. |
| ACTION_ITEM | 회의 후 수행해야 하는 업무 | 로그인 오류를 수정한다. |
| ASSIGNEE | 후속 업무 담당자 | 김 대리 |
| DEADLINE | 업무 완료 기한 | 금요일까지 |
| SCHEDULE | 업무 외 프로젝트 주요 일정 | 다음 주 월요일 배포 |
| PRIORITY | 업무 우선순위 | HIGH |
| PRIORITY_REASON | 우선순위 추천 근거 | 배포 전에 해결이 필요한 오류 |
| OTHER | 결정·업무로 보기 어려운 일반 의견 | 디자인이 조금 애매한 것 같다. |

### Classification Policy

- 명확하지 않은 내용을 임의로 결정 사항이나 후속 업무로 만들지 않는다.
- 담당자·마감일이 언급되지 않으면 `미지정` 으로 둔다.
- 상대적인 날짜 표현(금요일, 다음 주)은 **회의 날짜 기준**으로 절대 날짜로 바꾼다.
- 우선순위는 추천값이며 사용자가 수정할 수 있다.
- **AI 가 뽑은 담당자는 이름 문자열이다.** 확정 시 프로젝트 구성원과 매칭해 실제 사용자로 바꾸며, 실패하면 미지정으로 남긴다.

---

## 10. System Concept

```
┌──────────────────────────────┐
│   React (Vercel)             │
│   정적 호스팅 + /api 프록시   │
└──────────┬───────────────────┘
           │ REST (같은 오리진 /api → 프록시)
           │
           │   ┌── SSE 는 프록시를 거치면 스트림이 고여
           │   │   백엔드에 직접 연결한다
           ▼   ▼
┌──────────────────────────────┐
│   Spring Boot (EC2, HTTPS)   │
├──────────────────────────────┤
│ Authentication (이메일·구글)  │
│ Project / Member / Invitation│
│ Meeting                      │
│ AI Analysis                  │
│ Action Item                  │
│ Dashboard                    │
│ Notification (SSE · Push)    │
└────┬──────────┬─────────┬────┘
     ▼          ▼         ▼
┌─────────┐ ┌────────┐ ┌──────────┐
│  MySQL  │ │ LLM API│ │ FCM      │
└─────────┘ └────────┘ └──────────┘
```

**API 규모** — 공개 엔드포인트 31개 (OpenAPI 기준) + 푸시 구독 2개

### AI Processing Flow

```
회의록 → Spring Boot → LLM API 요청
      → 구조화된 결과 반환 → 응답 형식 검증
      → React 에 초안 전달 → 사용자 검토·수정
      → 확정 요청 → MySQL 저장
```

---

## 11. Data / Storage Policy

| 데이터 | 저장 | 비고 |
| --- | --- | --- |
| 사용자 계정 | 저장 | 이메일 또는 구글 계정 연결 |
| 프로젝트 / 구성원 | 저장 | OWNER · MEMBER |
| **초대** | 저장 | PENDING / ACCEPTED / CANCELLED, 만료 시각 |
| 회의 · 참여자 | 저장 | 제목, 일시, 참여자 |
| 원본 회의록 | 저장 | 회의 상세 조회 |
| 결정 사항 / 후속 업무 | 저장 | **사용자 확정 이후** |
| 업무 상태 · 우선순위 · 우선순위 이유 | 저장 | 사용자 수정 가능 |
| **알림** | 저장 | 종류, 읽음 시각 |
| **푸시 구독 토큰** | 저장 | 로그아웃 시 삭제 |
| 음성 파일 | **미저장** | 브라우저에서 텍스트로만 변환 |
| AI 요청·응답 원문 | 최소화 | 모델명·프롬프트 버전만 보관 |

> **Storage Principle** — AI 가 생성한 분석 결과는 사용자가 확정하기 전까지 실제 후속 업무 데이터로 취급하지 않는다.

---

## 12. Non-functional Requirements

### Performance

- 일반 CRUD API 는 사용 흐름을 방해하지 않는 응답 속도를 유지한다.
- AI 분석은 오래 걸리므로 별도 로딩 상태를 제공하고, 실패 시 재시도할 수 있다.
- **알림 폴링이 상시 트래픽이 되지 않게 한다** — SSE 연결 시 폴링 간격을 완화한다.

| 항목 | 개선 전 | 개선 후 | 효과 |
| --- | --- | --- | --- |
| 새 알림 표시까지 | 최대 15초 | 즉시 (SSE) | 체감 지연 제거 |
| 알림 폴링 (1인·1시간) | 240회 | 12회 | **95% 감소** |
| 업무 목록 폴링 (1인·1시간) | 60회 | 12회 | **80% 감소** |
| 앱 종료 중 알림 | 없음 | 웹 푸시 도달 | 경로 추가 |
| 푸시 SDK 가 첫 화면에 주는 영향 | — | 0 (동적 로딩) | 메인 번들 264.6 kB 유지 |

### Security

- 인증된 사용자만 기능을 사용할 수 있다.
- 프로젝트 접근 권한이 없으면 해당 데이터에 접근할 수 없다.
- **초대 토큰은 추측 불가능해야 하고, 한 번 수락하면 폐기된다.**
- **초대 수락은 로그인한 계정의 이메일이 초대받은 주소와 같을 때만 허용한다.**
- 구글 로그인은 ID 토큰을 **백엔드가 검증**하고 자체 토큰을 발급한다. 프론트는 구글 토큰을 인증 판단에 쓰지 않는다.
- Firebase 서비스 계정 키 등 서버 비밀값은 프론트 번들에 포함하지 않는다.

### Reliability

- AI API 실패가 전체 장애로 이어지지 않는다. 실패 시 재시도 가능(`FAILED`).
- AI 응답 형식이 올바르지 않으면 검증 후 오류 처리한다.
- **알림은 경로가 끊겨도 멈추지 않는다** — SSE 실패 시 폴링이 대신 받는다.
- **한쪽이 아직 배포되지 않아도 다른 쪽이 멈추지 않는다** — 프론트는 서버에 없는 기능을 감지하면 이전 동작으로 되돌아간다.

### Usability

- AI 분석 결과는 사용자가 직접 수정할 수 있어야 한다.
- 업무 상태와 우선순위를 한눈에 구분할 수 있어야 한다. **색은 장식이 아니라 상태를 의미한다.**
- 빈 상태 · 로딩 상태 · 오류 상태를 명확히 표시한다.
- **브라우저 알림 권한은 화면 진입 시 묻지 않는다.** 사용자가 직접 켤 때만 요청한다.

### Maintainability

- Frontend / Backend API 명세를 문서화한다 (OpenAPI + `BACKEND_NOTES.md`).
- 디자인 토큰을 문서로 고정한다 (`DESIGN.md` — 간격 8단계, 색 팔레트).
- 기능별 모듈 구조를 분리한다.

---

## 13. Exception Policy

| 상황 | 처리 정책 |
| --- | --- |
| 회의록 내용 없음 | AI 분석 버튼 비활성화 |
| 너무 짧은 회의록 | 분석 가능 여부 안내 |
| AI API 호출 실패 | `FAILED` 로 표시하고 재시도 제공 |
| AI 응답 형식 오류 | 검증 실패 처리 및 재시도 |
| 후속 업무 / 결정 사항 없음 | 빈 상태 표시 |
| 담당자·마감일 추출 실패 | `미지정` |
| AI 담당자 이름이 구성원과 매칭 안 됨 | 미지정으로 두고 사용자가 선택 |
| 이미 완료된 업무 | 다음 회의 미완료 목록에서 제외 |
| 마감일 경과 + 미완료 | 지연 업무로 표시, 알림 상시 유지 |
| **정원(10명) 초과 초대** | 추가 차단 및 안내 |
| **만료·취소된 초대 링크** | 사유를 구분해 안내하고 재발송 요청 유도 |
| **초대와 다른 계정으로 로그인** | 수락 차단, 어느 계정으로 들어와야 하는지 안내 |
| **브라우저 알림 차단 상태** | 해제 방법 안내 (다시 묻지 않음) |
| **서버에 아직 없는 기능 호출** | 이전 동작으로 되돌아가고 로그아웃시키지 않음 |
| 권한 없는 프로젝트 접근 | 접근 거부 |
| 존재하지 않는 리소스 | Not Found 처리 |
| 서버 오류 | 공통 오류 메시지 표시 |

---

## 14. Success Metrics

### MVP Completion

- Must 기능 구현률 **100%**
- 핵심 사용자 흐름 전체 동작
- 운영 환경 배포 완료 — **매일 배포, 푸시 시 자동 배포**
- 주요 QA 시나리오 통과

### AI Validation

테스트 회의록 기준으로 검증한다 — 결정 사항 / 후속 업무 / 담당자 / 마감일 / 주요 일정 추출 여부, 업무가 아닌 문장의 과도한 업무 생성 여부.

### 추가 지표 *(v2.0 신규)*

| 지표 | 정의 | 목적 |
| --- | --- | --- |
| **확정 전 수정 비율** | AI 초안 항목 중 사용자가 고친 비율 | 낮을수록 검토 단계가 짧아진다 — 제품 핵심 가치와 직결 |
| **알림 도달률** | 발송 대비 실제 표시 | SSE·푸시 경로가 제 역할을 하는지 |
| **초대 수락률** | 보낸 초대 중 수락 | 온보딩 마찰 확인 |

### User Flow (중단 없이 완료되어야 함)

```
프로젝트 생성 → 구성원 초대·수락 → 회의 생성 → 회의록 작성
→ AI 분석 → 결과 수정 → 확정 → 후속 업무 생성
→ 상태 변경 → 알림 수신 → 다음 회의에서 미완료 업무 확인
```

---

## 15. Acceptance Criteria

| ID | Given | When | Then |
| --- | --- | --- | --- |
| AC-01 | 프로젝트에 접근한 상태 | 새 회의를 생성하면 | 회의 목록에 추가된다 |
| AC-02 | 정상적인 회의록이 있는 상태 | AI 분석을 요청하면 | 결정 사항·후속 업무 결과가 표시된다 |
| AC-03 | AI 분석이 완료된 상태 | 결과 화면에 진입하면 | 확정된 업무가 아닌 **수정 가능한 초안**으로 표시된다 |
| AC-04 | 분석 결과가 생성된 상태 | 담당자·마감일·업무명·우선순위를 바꾸면 | 수정 내용 기준으로 확정할 수 있다 |
| AC-05 | 결과를 검토한 상태 | 확정하면 | **초안과 확정본 차이를 먼저 보여주고**, 확인 후 후속 업무로 저장된다 |
| AC-06 | 후속 업무가 존재할 때 | 상태를 변경하면 | 예정/진행 중/완료 중 선택한 상태로 저장된다 |
| AC-07 | 마감일이 지난 미완료 업무가 있을 때 | 대시보드·업무 목록에 접근하면 | 지연으로 표시되고 알림이 유지된다 |
| AC-08 | 이전 회의의 미완료 업무가 있을 때 | 다음 회의를 생성하면 | 그 업무를 확인하고 이어받을 수 있다 |
| AC-09 | AI 호출이 실패했을 때 | 분석을 완료할 수 없으면 | 실패 상태와 재시도 방법을 제공한다 |
| **AC-10** | 미가입자를 이메일로 초대했을 때 | 그 사람이 메일 링크로 가입하면 | **버튼을 한 번 더 누르지 않아도** 해당 프로젝트에 합류한다 |
| **AC-11** | 초대가 취소·만료됐을 때 | 링크로 접근하면 | 사유를 구분해 안내하고 수락을 막는다 |
| **AC-12** | 브라우저 알림을 켠 상태 | 앱을 완전히 종료한 뒤 업무가 배정되면 | 데스크톱 알림이 도착하고, 누르면 해당 업무 화면이 열린다 |
| **AC-13** | 서버에 아직 없는 기능을 호출할 때 | 401/404 가 돌아오면 | **로그아웃되지 않고** 이전 동작으로 되돌아간다 |

---

## 16. Out of Scope

화상회의 · 실시간 음성 회의 · 자체 STT 모델 · 자체 LLM 학습 · 실시간 공동 편집 · 실시간 채팅 · 모바일 앱 · Slack/Notion/Jira 양방향 연동 · 복잡한 기업 조직 권한 체계 · ML 기반 지연 예측 · 자동 수행 AI Agent · 외부 캘린더 연동

> 음성 인식은 **브라우저 내장 기능 사용**으로 범위 안에 들어왔다. 자체 STT 모델 개발은 여전히 범위 밖이다.

---

## 17. Roadmap

### Phase 1 — MVP **(완료)**
인증(이메일·구글) · 프로젝트 · **구성원 초대** · 회의 · AI 분석 · 후속 업무(다중 담당자) · 이전 회의 연결 · 대시보드

### Phase 1.5 — Input / Notification **(앞당겨 완료)**
음성 받아쓰기 · 서비스 내 알림(SSE + 폴링 안전망) · **웹 푸시**

### Phase 2 — 남은 것
- 계정 관리 서버 API (이름 변경 · 탈퇴)
- 합류 알림(`MEMBER_JOINED`) 서버 발송
- 회의록 파일 업로드
- 이메일 업무 알림 · 캘린더 연동
- **AI 추출 정확도 측정** (확정 전 수정 비율)

### Phase 3 — AI Intelligence
AI 다음 회의 안건 추천 · 프로젝트 회고 자동 생성 · 반복 논의 주제 분석 · 장기 미완료 업무 분석 · 담당자별 업무 부하 분석

### Phase 4 — External Collaboration
Slack · Notion · Jira · GitHub · 외부 캘린더 연동

---

## 18. Final Product Direction

> **FollowUp 은 단순한 AI 회의록 요약 서비스를 목표로 하지 않는다.**

회의에서 나온 결정 사항과 후속 업무를 구조화하고, 이를 실제 업무 관리로 연결하며, 완료되지 않은 업무를 다음 회의까지 이어서 관리하는 **AI 기반 회의 후속 업무 관리 서비스**를 목표로 한다.

AI 는 사용자를 대신해 최종 결정을 내리지 않는다. 비정형 회의 내용을 구조화하고 반복적인 정리 작업을 줄이는 역할만 한다. 최종 업무 생성과 수정은 사용자가 결정하도록 설계해 AI 의 불확실성을 보완한다.

```
회의 → AI 구조화 → 사용자 검토 → 후속 업무 → 실행 관리 → 다음 회의 연결
```

개발을 거치며 한 가지가 더해졌다. 회의와 회의 사이, **화면을 열지 않는 동안에도 업무가 잊히지 않게 하는 것**이다. 그래서 알림을 Phase 2 에서 MVP 로 끌어올렸다.

핵심 방향은 **"회의를 잘 기록하는 서비스"가 아니라 "회의에서 나온 일이 실제로 실행되도록 만드는 서비스"** 이다.
